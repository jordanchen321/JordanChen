/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/JordanChen",
  assetPrefix: "/JordanChen/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
